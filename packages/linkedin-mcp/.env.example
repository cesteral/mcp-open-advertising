# LinkedIn Ads MCP Server Configuration

# Server
LINKEDIN_MCP_PORT=3006
LINKEDIN_MCP_HOST=127.0.0.1
NODE_ENV=development

# Auth mode: linkedin-bearer (default), jwt, none
MCP_AUTH_MODE=linkedin-bearer

# JWT auth (only for jwt mode)
# MCP_AUTH_SECRET_KEY=your-secret-key

# LinkedIn API
LINKEDIN_API_BASE_URL=https://api.linkedin.com
# LINKEDIN_API_VERSION pins the LinkedIn-Version header. Leave it UNSET so the
# vetted default in src/config/api-version.ts applies. Any value set here
# overrides that pin, and LinkedIn errors on every call once a version is past
# its support window (a copied `202501` did exactly that).
# LINKEDIN_API_VERSION=YYYYMM
# Leave unset to use the default (10/min per process). No published quota backs a
# higher value — see linkedin.rate_limit_default in platform-facts.json before raising.
# LINKEDIN_RATE_LIMIT_PER_MINUTE=10

# Stdio mode credentials
LINKEDIN_ACCESS_TOKEN=your-access-token-here

# Refresh token flow (recommended for production)
# LINKEDIN_CLIENT_ID=your-client-id
# LINKEDIN_CLIENT_SECRET=your-client-secret
# LINKEDIN_REFRESH_TOKEN=your-refresh-token

# Session Management
# MCP_STATEFUL_SESSION_TIMEOUT_MS=3600000

# CORS
# MCP_ALLOWED_ORIGINS=https://app.example.com

# Logging
LOG_LEVEL=info
# MCP_LOG_LEVEL=debug

# OpenTelemetry (optional)
# OTEL_ENABLED=true
# OTEL_SERVICE_NAME=linkedin-mcp
# OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=http://localhost:4318/v1/traces
# OTEL_EXPORTER_OTLP_METRICS_ENDPOINT=http://localhost:4318/v1/metrics

# GCS Persistence (optional)
# GCS_BUCKET_NAME=your-bucket-name
