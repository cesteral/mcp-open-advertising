# =============================================================================
# cm360-mcp Environment Configuration Template
# =============================================================================
# Copy this file to .env and fill in your values
# Required variables are marked with [REQUIRED]
# Optional variables show their default values

# =============================================================================
# Service Configuration
# =============================================================================

# Service identifier used in logging and telemetry
SERVICE_NAME=cm360-mcp

# HTTP server configuration
CM360_MCP_PORT=3008
CM360_MCP_HOST=0.0.0.0

# Environment mode (affects logging, error handling, auth enforcement)
NODE_ENV=development  # Options: development | production | test

# =============================================================================
# CM360 API Authentication [REQUIRED]
# =============================================================================

# Google Cloud Service Account credentials for CM360 API access
# Choose ONE of the following options:

# OPTION 1: Path to service account JSON file (RECOMMENDED for local development)
CM360_SERVICE_ACCOUNT_FILE=

# OPTION 2: Base64-encoded service account JSON (for CI/CD, containers)
CM360_SERVICE_ACCOUNT_JSON=

# OPTION 3: GCP Secret Manager secret ID (for production Cloud Run)
SERVICE_ACCOUNT_SECRET_ID=

# =============================================================================
# MCP Client Authentication
# =============================================================================

# Authentication mode for MCP clients connecting to this server
MCP_AUTH_MODE=none  # Options: none | jwt | google-headers

# JWT secret key (required if MCP_AUTH_MODE=jwt)
MCP_AUTH_SECRET_KEY=

# =============================================================================
# Session Management
# =============================================================================

MCP_SESSION_MODE=auto  # Options: stateless | stateful | auto
MCP_STATEFUL_SESSION_TIMEOUT_MS=3600000  # Default: 1 hour

# =============================================================================
# CORS Configuration
# =============================================================================

MCP_ALLOWED_ORIGINS=

# =============================================================================
# CM360 API Configuration
# =============================================================================

# CM360 API base URL (typically no need to change)
CM360_API_BASE_URL=https://dfareporting.googleapis.com/dfareporting/v5

# Rate limiting for CM360 API requests
CM360_RATE_LIMIT_PER_MINUTE=50

# =============================================================================
# Logging Configuration
# =============================================================================

LOG_LEVEL=info
MCP_LOG_LEVEL=debug

# =============================================================================
# OpenTelemetry (Optional)
# =============================================================================

OTEL_ENABLED=false
OTEL_SERVICE_NAME=cm360-mcp
OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=
OTEL_EXPORTER_OTLP_METRICS_ENDPOINT=
