# =============================================================================
# ttd-mcp Environment Configuration Template
# =============================================================================
# Copy this file to .env and fill in your values
# Required variables are marked with [REQUIRED]
# Optional variables show their default values

# =============================================================================
# Service Configuration
# =============================================================================

# Service identifier used in logging and telemetry
SERVICE_NAME=ttd-mcp

# HTTP server configuration
TTD_MCP_PORT=3003
TTD_MCP_HOST=0.0.0.0

# Environment mode (affects logging, error handling, auth enforcement)
NODE_ENV=development  # Options: development | production | test

# =============================================================================
# TTD API Authentication [REQUIRED]
# =============================================================================

# The Trade Desk Partner ID and API Secret
# These are used for stdio mode (env var credentials) and as fallback for HTTP mode
TTD_PARTNER_ID=
TTD_API_SECRET=

# =============================================================================
# MCP Client Authentication
# =============================================================================

# Authentication mode for MCP clients connecting to this server
MCP_AUTH_MODE=none  # Options: ttd-headers | jwt | none

# JWT secret key (required if MCP_AUTH_MODE=jwt)
# Generate with: openssl rand -base64 32
MCP_AUTH_SECRET_KEY=

# =============================================================================
# Session Management
# =============================================================================

# Session mode for MCP connections
MCP_SESSION_MODE=auto  # Options: stateless | stateful | auto

# Stateful session timeout (milliseconds)
MCP_STATEFUL_SESSION_TIMEOUT_MS=3600000  # Default: 1 hour

# =============================================================================
# CORS Configuration
# =============================================================================

# Comma-separated list of allowed origins for CORS
MCP_ALLOWED_ORIGINS=

# =============================================================================
# TTD API Configuration
# =============================================================================

# TTD API base URL (typically no need to change)
TTD_API_BASE_URL=https://api.thetradedesk.com/v3

# TTD Auth token endpoint
TTD_AUTH_URL=https://auth.thetradedesk.com/oauth2/token

# Rate limiting for TTD API requests
TTD_RATE_LIMIT_PER_MINUTE=100

# =============================================================================
# Logging Configuration
# =============================================================================

# General log level
LOG_LEVEL=info  # Options: debug | info | notice | warning | error

# MCP-specific log level
MCP_LOG_LEVEL=debug  # Options: debug | info | notice | warning | error

# =============================================================================
# OpenTelemetry (Optional)
# =============================================================================

# Enable OpenTelemetry instrumentation
OTEL_ENABLED=false

# Service name for telemetry (defaults to SERVICE_NAME if not set)
OTEL_SERVICE_NAME=ttd-mcp

# OTLP exporter endpoints
OTEL_EXPORTER_OTLP_TRACES_ENDPOINT=
OTEL_EXPORTER_OTLP_METRICS_ENDPOINT=
