# Backend configuration for prod environment
# Usage: terraform init -backend-config=backend-prod.conf

bucket = "open-agentic-advertising-prod-terraform-state" # TODO: Create this bucket first
prefix = "terraform/state"
