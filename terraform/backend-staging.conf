# Backend configuration for staging environment
# Usage: terraform init -backend-config=backend-staging.conf

bucket = "bidshifter-staging-terraform-state" # TODO: Create this bucket first
prefix = "terraform/state"
