# Backend configuration for dev environment
# Usage: terraform init -backend-config=backend-dev.conf

bucket = "bidshifter-dev-terraform-state" # TODO: Create this bucket first
prefix = "terraform/state"
